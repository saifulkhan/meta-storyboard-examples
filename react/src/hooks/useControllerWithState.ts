/**
 * re-export of the hook shipped with the library; when using the published
 * package import it from 'meta-storyboard/react' instead
 */
export { useControllerWithState } from '../../msb/src/react';
export type { ControllerLike } from '../../msb/src/react';
